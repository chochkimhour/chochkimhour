{
  "name": "github-profile",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub profile README for Choch Kimhour (Andrew6rant-style SVG card).",
  "type": "module",
  "scripts": {
    "generate:svg": "node scripts/generate-profile-svg.mjs",
    "generate": "node scripts/generate-readme.mjs",
    "generate:all": "npm run generate:svg && npm run generate",
    "check": "node scripts/generate-readme.mjs --check",
    "validate": "npm run validate:readme && npm run validate:example",
    "validate:readme": "node scripts/validate-readme.mjs",
    "validate:example": "node scripts/generate-readme.mjs --env .env.example --output README.example.md --dry-run"
  },
  "engines": {
    "node": ">=20"
  }
}
